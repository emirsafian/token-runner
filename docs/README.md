# docs

Screenshots and the demo clip live here.

Record the demo with `tokenrun --demo`, screen-grab it, and save it as
`docs/tokenrun.gif`. The main README points at that path, so it shows up at the top
once the file is here. A still `docs/tokenrun.gif` can be a real GIF or just an
exported frame.
